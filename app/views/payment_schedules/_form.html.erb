<%= form_with(model: payment_schedule, local: true) do |form| %>
  <% if payment_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment_schedule.errors.count, "error") %> prohibited this payment_schedule from being saved:</h2>

      <ul>
      <% payment_schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :document_id %>
    <%= form.text_field :document_id, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if payment_schedule.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', payment_schedule, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if payment_schedule.persisted? %>
      <%= link_to "Cancel", payment_schedule, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", payment_schedules_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
