json.extract! agreement, :id, :provider_rep_name, :provider_signature, :provider_signed_on, :client_rep_name, :client_rep_signature, :client_signed_on, :signature, :signed_on, :user_id, :document_id, :created_at, :updated_at
json.url agreement_url(agreement, format: :json)
